import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Strict mode for early detection of potential issues in React components
  reactStrictMode: true,
};

export default nextConfig;
