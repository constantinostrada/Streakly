# ─── App ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_NAME="Habit Tracker"

# ─── Persistence (choose one and configure in infrastructure/) ────────────────
# In-memory store is the default — no configuration required.
# To swap to a real DB, implement IHabitRepository in infrastructure/ and update
# the factory in src/infrastructure/persistence/repositoryFactory.ts

# DATABASE_URL="postgresql://user:password@localhost:5432/habit_tracker"
